import { render } from 'react-dom';
import FindFriendsScreen from './FindFriendsScreen';

describe('Test channel data', () => {
  test(`creates channel type`, async () => {
    const screen = render(<FindFriendsScreen />);
  });
});
